wandb:
  run_name: 
api: openai # openai, anthropic, google,..
batch_size: 32
model:
  pretrained_model_name_or_path:  #if you use openai api, put the name of model
  bfcl_model_id:  # find id from "llm-leaderboard/scripts/evaluator/evaluate_utils/bfcl_pkg/SUPPORTED_MODELS.md"
  base_model: "unknown"
  size_category: api
  size: null
  release_date: # MM/DD/YYYY

